---
# default variables
# please overwrite in the playbook.yml

deployer_username: deployer
deployer_password: $1$BYnbBLbt$uPcFu8SPIZdxqPh61
deployer_public_key: "{{ lookup('file', '/Users/filips01/.ssh/id_dsa.pub' ) }}"

groups_to_join:
  - sudo
  - ssh
  - www-data
